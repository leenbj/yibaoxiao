# ============================================
# 易报销 Pro 环境变量示例（2核4G 小机型推荐）
# 请复制为 .env 并填写真实值
# ============================================

# 基本
NODE_ENV=production
NODE_OPTIONS="--max-old-space-size=512 --optimize_for_size --max_semi_space_size=16"

# 数据库（PostgreSQL）
# 可选：使用 DATABASE_URL（优先级高于分项配置）
# DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_NAME=yibao
DATABASE_USER=yibao
DATABASE_PASSWORD=请修改为强密码
# 连接池调优（2核4G 推荐）
DATABASE_POOL_MAX=3
DATABASE_POOL_MIN=0
DATABASE_IDLE_TIMEOUT=5000
DATABASE_CONNECT_TIMEOUT=5000

# AI 默认配置（至少 provider + apiKey，否则返回模拟数据）
DEFAULT_AI_PROVIDER=
DEFAULT_AI_API_KEY=
DEFAULT_AI_API_URL=
DEFAULT_AI_MODEL=
# 共享配置/兜底用户（管理员 ID，如 user_wangbo）
ADMIN_USER_ID=

# 管理员默认账号（请按需修改）
ADMIN_EMAIL=wangbo@knet.cn
ADMIN_PASSWORD=123456
ADMIN_NAME=王波
ADMIN_DEPARTMENT=管理部

# GHCR 命名空间（docker-compose 拉取镜像用；通常为 GitHub 用户名/Org）
# DOCKER_NAMESPACE=your-ghcr-namespace
