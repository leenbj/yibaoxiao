# ============================================================
# 易报销 Pro - 生产环境配置示例
# ============================================================
#
# 使用方法：
#   1. 复制此文件为 .env
#      cp .env.production.example .env
#   2. 修改下方配置为实际值
#   3. 确保 .env 已添加到 .gitignore（不要提交到 Git）
#
# ============================================================

# ==================== 镜像配置 ====================
# GitHub Container Registry 镜像地址
BACKEND_IMAGE=ghcr.io/leenbj/yibaoxiao-backend:latest
FRONTEND_IMAGE=ghcr.io/leenbj/yibaoxiao-frontend:latest

# ==================== 数据库配置 ====================
# PostgreSQL 配置
POSTGRES_USER=yibao
POSTGRES_PASSWORD=请修改为强密码
POSTGRES_DB=yibao

# 数据库连接池配置
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=20
DATABASE_IDLE_TIMEOUT=60000
DATABASE_CONNECT_TIMEOUT=10000

# ==================== 管理员配置 ====================
# 超级管理员账号（首次启动时创建）
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=请修改为强密码
ADMIN_NAME=系统管理员
ADMIN_DEPARTMENT=管理部
ADMIN_USER_ID=user_admin

# ==================== AI 配置（可选）====================
# AI 服务提供商（openai, anthropic, gemini 等）
DEFAULT_AI_PROVIDER=

# AI API 密钥
DEFAULT_AI_API_KEY=

# AI API 地址（可选，使用默认值）
DEFAULT_AI_API_URL=

# AI 模型名称
DEFAULT_AI_MODEL=

# ==================== 应用配置 ====================
# 端口配置
FRONTEND_PORT=80
BACKEND_PORT=3000

# 日志级别（debug, info, warn, error）
LOG_LEVEL=info

# Motia 配置
MOTIA_DISABLE_DB_SYNC=false

# ==================== 高级配置（通常不需要修改）====================
# Node.js 内存配置
NODE_OPTIONS=--max-old-space-size=768

# 数据库缓存
DATABASE_CACHE_ENABLED=false

# ============================================================
# 配置说明
# ============================================================
#
# 必须修改的配置：
#   ✅ POSTGRES_PASSWORD - 数据库密码（强密码）
#   ✅ ADMIN_EMAIL - 管理员邮箱
#   ✅ ADMIN_PASSWORD - 管理员密码（强密码）
#   ✅ ADMIN_NAME - 管理员姓名
#
# 可选配置：
#   - AI 配置：如需使用 AI 功能，填写相关配置
#   - 端口配置：如有端口冲突，修改端口
#   - 日志级别：生产环境建议使用 info 或 warn
#
# 安全建议：
#   1. 使用强密码（至少 12 位，包含大小写字母、数字、特殊字符）
#   2. 定期更换密码
#   3. 不要将 .env 文件提交到版本控制
#   4. 限制 .env 文件的访问权限（chmod 600 .env）
#
# ============================================================
