name: Build Docker Images

# 易报销 Pro - Docker 镜像构建工作流
# 构建镜像并导出为 artifact，供本地下载后推送到 Docker Hub

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

jobs:
  build-backend:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver: docker

      - name: Build Backend image
        run: |
          echo "🔨 开始构建后端镜像..."
          docker build \
            --file Dockerfile.backend \
            --tag yibao-backend:latest \
            --build-arg NODE_OPTIONS="--max-old-space-size=4096" \
            .
          echo "✅ 构建完成"

      - name: Save and compress image
        run: |
          echo "📦 导出镜像..."
          docker save yibao-backend:latest -o /tmp/yibao-backend.tar
          echo "🗜️ 压缩镜像..."
          gzip -9 /tmp/yibao-backend.tar
          ls -lh /tmp/yibao-backend.tar.gz

      - name: Upload Backend image artifact
        uses: actions/upload-artifact@v4
        with:
          name: yibao-backend-image
          path: /tmp/yibao-backend.tar.gz
          retention-days: 7
          compression-level: 0

      - name: Summary
        run: |
          echo "## ✅ 后端镜像构建完成" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          SIZE=$(ls -lh /tmp/yibao-backend.tar.gz | awk '{print $5}')
          echo "镜像大小: \`$SIZE\`" >> $GITHUB_STEP_SUMMARY

  build-frontend:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver: docker

      - name: Build Frontend image
        run: |
          echo "🔨 开始构建前端镜像..."
          docker build \
            --file Dockerfile.frontend \
            --tag yibao-frontend:latest \
            .
          echo "✅ 构建完成"

      - name: Save and compress image
        run: |
          echo "📦 导出镜像..."
          docker save yibao-frontend:latest -o /tmp/yibao-frontend.tar
          echo "🗜️ 压缩镜像..."
          gzip -9 /tmp/yibao-frontend.tar
          ls -lh /tmp/yibao-frontend.tar.gz

      - name: Upload Frontend image artifact
        uses: actions/upload-artifact@v4
        with:
          name: yibao-frontend-image
          path: /tmp/yibao-frontend.tar.gz
          retention-days: 7
          compression-level: 0

      - name: Summary
        run: |
          echo "## ✅ 前端镜像构建完成" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          SIZE=$(ls -lh /tmp/yibao-frontend.tar.gz | awk '{print $5}')
          echo "镜像大小: \`$SIZE\`" >> $GITHUB_STEP_SUMMARY

  summary:
    needs: [build-backend, build-frontend]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Final Summary
        run: |
          echo "## 🎉 构建完成！" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 下载镜像" >> $GITHUB_STEP_SUMMARY
          echo "在本页面的 **Artifacts** 区域下载：" >> $GITHUB_STEP_SUMMARY
          echo "- \`yibao-backend-image\` (后端镜像)" >> $GITHUB_STEP_SUMMARY
          echo "- \`yibao-frontend-image\` (前端镜像)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🚀 本地推送到 Docker Hub" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "cd /path/to/yibao" >> $GITHUB_STEP_SUMMARY
          echo "mv ~/Downloads/yibao-*-image.zip ." >> $GITHUB_STEP_SUMMARY
          echo "./scripts/push-to-dockerhub.sh YOUR_DOCKER_USERNAME" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 👤 默认管理员" >> $GITHUB_STEP_SUMMARY
          echo "- 邮箱: \`wangbo@knet.cn\`" >> $GITHUB_STEP_SUMMARY
          echo "- 密码: \`123456\`" >> $GITHUB_STEP_SUMMARY
