name: Build Docker Images

# 易报销 Pro - Docker 镜像构建工作流
# 构建镜像并导出为 artifact，供本地下载后推送到 Docker Hub

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:
    inputs:
      build_backend:
        description: '构建后端镜像'
        required: false
        default: true
        type: boolean
      build_frontend:
        description: '构建前端镜像'
        required: false
        default: true
        type: boolean

jobs:
  build-backend:
    if: ${{ github.event.inputs.build_backend != 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Backend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.backend
          tags: yibao-backend:latest
          outputs: type=docker,dest=/tmp/yibao-backend.tar
          cache-from: type=gha,scope=backend
          cache-to: type=gha,mode=max,scope=backend
          build-args: |
            NODE_OPTIONS=--max-old-space-size=4096

      - name: Compress image
        run: |
          gzip -9 /tmp/yibao-backend.tar
          ls -lh /tmp/yibao-backend.tar.gz

      - name: Upload Backend image artifact
        uses: actions/upload-artifact@v4
        with:
          name: yibao-backend-image
          path: /tmp/yibao-backend.tar.gz
          retention-days: 7
          compression-level: 0  # 已经压缩过了

      - name: Summary
        run: |
          echo "## ✅ 后端镜像构建完成" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "镜像文件: \`yibao-backend.tar.gz\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 下载和推送步骤" >> $GITHUB_STEP_SUMMARY
          echo "1. 在 Actions 页面下载 \`yibao-backend-image\` artifact" >> $GITHUB_STEP_SUMMARY
          echo "2. 解压并加载: \`gunzip yibao-backend.tar.gz && docker load -i yibao-backend.tar\`" >> $GITHUB_STEP_SUMMARY
          echo "3. 打标签: \`docker tag yibao-backend:latest YOUR_USERNAME/yibao-backend:latest\`" >> $GITHUB_STEP_SUMMARY
          echo "4. 推送: \`docker push YOUR_USERNAME/yibao-backend:latest\`" >> $GITHUB_STEP_SUMMARY

  build-frontend:
    if: ${{ github.event.inputs.build_frontend != 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Frontend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.frontend
          tags: yibao-frontend:latest
          outputs: type=docker,dest=/tmp/yibao-frontend.tar
          cache-from: type=gha,scope=frontend
          cache-to: type=gha,mode=max,scope=frontend

      - name: Compress image
        run: |
          gzip -9 /tmp/yibao-frontend.tar
          ls -lh /tmp/yibao-frontend.tar.gz

      - name: Upload Frontend image artifact
        uses: actions/upload-artifact@v4
        with:
          name: yibao-frontend-image
          path: /tmp/yibao-frontend.tar.gz
          retention-days: 7
          compression-level: 0

      - name: Summary
        run: |
          echo "## ✅ 前端镜像构建完成" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "镜像文件: \`yibao-frontend.tar.gz\`" >> $GITHUB_STEP_SUMMARY

  summary:
    needs: [build-backend, build-frontend]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Final Summary
        run: |
          echo "## 🎉 构建完成！" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 下载镜像" >> $GITHUB_STEP_SUMMARY
          echo "在本页面的 **Artifacts** 区域下载：" >> $GITHUB_STEP_SUMMARY
          echo "- \`yibao-backend-image\` (后端镜像)" >> $GITHUB_STEP_SUMMARY
          echo "- \`yibao-frontend-image\` (前端镜像)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🚀 本地推送到 Docker Hub" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "# 下载 artifact 后执行" >> $GITHUB_STEP_SUMMARY
          echo "./scripts/push-to-dockerhub.sh YOUR_DOCKER_USERNAME" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 👤 默认管理员" >> $GITHUB_STEP_SUMMARY
          echo "- 邮箱: \`wangbo@knet.cn\`" >> $GITHUB_STEP_SUMMARY
          echo "- 密码: \`123456\`" >> $GITHUB_STEP_SUMMARY
