# 易报销 Pro - 环境变量配置示例
# 复制此文件为 .env 并修改配置
# cp .env.example .env

# ==================== 数据库配置 ====================
POSTGRES_USER=yibao
POSTGRES_PASSWORD=yibao123456
POSTGRES_DB=yibao

# 数据库连接 URL（Docker 环境自动生成，无需手动配置）
# DATABASE_URL=postgresql://yibao:yibao123456@postgres:5432/yibao

# ==================== 超级管理员配置 ====================
# 首次启动时自动创建超级管理员账户
ADMIN_EMAIL=wangbo@knet.cn
ADMIN_PASSWORD=123456
ADMIN_NAME=王波
ADMIN_DEPARTMENT=管理部
ADMIN_USER_ID=user_wangbo

# ==================== AI 配置（可选）====================
# 支持的 AI 厂商: gemini, deepseek, minimax, glm, openai, claude, qwen, moonshot, doubao, volcengine

# 默认 AI 配置（所有用户共享）
# DEFAULT_AI_PROVIDER=doubao
# DEFAULT_AI_API_KEY=your-api-key-here
# DEFAULT_AI_API_URL=https://ark.cn-beijing.volces.com/api/v3
# DEFAULT_AI_MODEL=ep-xxxxx

# ==================== 服务端口配置 ====================
# 前端服务端口（默认 80）
FRONTEND_PORT=80

# ==================== Node.js 配置 ====================
# 内存限制（MB），针对 4G 内存服务器建议 1024
NODE_OPTIONS=--max-old-space-size=1024

# ==================== 数据库连接池配置（性能优化）====================
# 最小连接数（预热连接，避免冷启动延迟）
DATABASE_POOL_MIN=2
# 最大连接数（并发处理能力）
DATABASE_POOL_MAX=10
# 空闲连接超时（毫秒）
DATABASE_IDLE_TIMEOUT=60000
# 连接超时（毫秒）
DATABASE_CONNECT_TIMEOUT=10000
# 连接最大生命周期（毫秒，用于负载均衡）
DATABASE_MAX_LIFETIME=3600000

# ==================== 查询缓存配置 ====================
# 启用查询缓存（默认 true）
DATABASE_CACHE_ENABLED=true
# 全局缓存模式（false=显式缓存，需要 .$withCache()）
DATABASE_CACHE_GLOBAL=false
# 缓存 TTL（秒）
DATABASE_CACHE_TTL=60
